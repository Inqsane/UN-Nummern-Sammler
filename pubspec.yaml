name: un_sammler
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.11.4
dependencies:
  flutter:
    sdk: flutter
  intl: ^0.18.1
  cupertino_icons: ^1.0.8
  url_launcher: ^6.1.10
  shared_preferences: ^2.1.1
  http: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.11.0
  flutter_lints: ^6.0.0

flutter_icons:
  android: true
  ios: true
  image_path: "assets/symbole/icon.png"

flutter_lints: ^6.0.0

flutter:
  assets:
    - assets/un_with_class.json
    - assets/symbole/ 

  uses-material-design: true
